import '../styles/instructions.scss'
import Layout from '../core/global/layout/layout';

export default function About() {
    return (
        <Layout title="About">
            <div className="about">
                <p>This will be about, but for now I don't know what I'm talking about.</p>
            </div>            
        </Layout>
    );
}